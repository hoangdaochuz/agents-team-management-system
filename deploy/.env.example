# Copy to .env and fill in. Never commit .env.
# Envelope key for encrypting provider API keys at rest (Phase 1+).
ENCRYPTION_KEY=change-me-32-bytes-of-randomness
# GitHub token for gh / PR creation (Phase 6+).
GITHUB_TOKEN=
