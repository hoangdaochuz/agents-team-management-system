apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Argo CD itself is installed once per cluster from the pinned upstream
  # manifest (see k8s/README.md bootstrap); everything below is our
  # GitOps delivery graph (app-of-apps + operators + configs + workloads).
  - apps/
