apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Argo CD installation from upstream manifest
  # Pinned to v2.12.4 - update via bump in comments
  - github.com/argoproj/argo-cd manifests/base install.yaml
  - apps/
