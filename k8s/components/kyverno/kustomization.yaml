apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Kyverno installation from upstream
  # Pinned to v1.12.4 - update via bump in comments
  - github.com/kyverno/kyverno manifests/release/install.yaml
  - policies/
