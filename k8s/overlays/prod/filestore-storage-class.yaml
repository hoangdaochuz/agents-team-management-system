apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: filestore
provisioner: filestorage.csi.storage.gke.io
parameters:
  tier: premium  # Use premium tier for prod
  network: default
volumeBindingMode: WaitForFirstConsumer
allowVolumeExpansion: true
